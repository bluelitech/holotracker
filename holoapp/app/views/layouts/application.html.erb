<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= page_title(yield(:title), @locale) %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="ホロライブメンバーのチャンネル登録者数の遷移を折れ線グラフで細かく比較することが出来ます。また、最新の登録者数の一覧ではランキングとして確認・比較することも可能です。">
    <meta name="twitter:title" content="ホロとらっかー - ホロライブのチャンネル登録者数の遷移と一覧">
    <meta name="twitter:description" content="ホロライブメンバーのチャンネル登録者数の遷移を折れ線グラフで細かく比較することが出来ます。また、最新の登録者数の一覧ではランキングとして確認・比較することも可能です。">
    <meta name="twitter:image" content="https://trackholo.live/img/ranking.png">
    <meta name="twitter:card" content="summary_large_image">
    <%= favicon_link_tag('favicon.ico') %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="alternate" hreflang="ja" href="https://trackholo.live/">
    <link rel="alternate" hreflang="en" href="https://trackholo.live/en/">
    <%
=begin%>
    <script data-ad-client="ca-pub-3704681793677761" async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-100810523-8"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'UA-100810523-8');
    </script> 
<%
=end%>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
  <% @members.each do |member| %>
    <input
      type="hidden"
      member_id="<%= member.id %>"
      name="<% if @locale == 'en' %><%= member.name_en %><% else %><%= member.name %><% end %>"
      name_kana="<%= member.name_kana %>"
      name_url="<%= member.name_url %>"
      belong="<% if @locale == 'en' %><%= member.belong_en %><% else %><%= member.belong %><% end %>"
      created_at="<%= member.created_at %>"
      debut="<%= member.debut %>"
      class="member_data" />
  <% end %>
</html>
